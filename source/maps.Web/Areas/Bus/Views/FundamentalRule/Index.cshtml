@model IEnumerable<FundamentalRule>
@{
    ViewBag.Title = "Основні правила";
    Layout = "~/Areas/Bus/Views/Shared/_AdminLayout.cshtml";
}
<h2>
    @ViewBag.Title
</h2>
@Html.ActionLink("Додати", "Create", "FundamentalRule", null, new { @class = "btn btn-primary" })
<table class="table">
    <thead>
        <tr>
            <th>
                Назва
            </th>
            <th>
            </th>
        </tr>
    </thead>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Name
            </td>
            <td>
                @Html.ActionLink("Редагувати", "Edit", "FundamentalRule", new { id = item.ID }, new { @class = "btn btn-xs btn-default" })
                @Html.ActionLink("Видалити", "Delete", "FundamentalRule", new { id = item.ID }, new { @class = "btn btn-xs delete-btn-style btn-danger" })
            </td>
        </tr>
    }
</table>

